[pytest]
# Only tests/ is the suite. data/tests/ holds live-network connectivity probes
# (nba_api reachability, basketball_reference, ESPN) -- scripts that print rather
# than assert, and that need scraper deps not in requirements.txt. Collecting them
# makes `pytest` fail on a fresh clone for reasons unrelated to the code.
testpaths = tests
